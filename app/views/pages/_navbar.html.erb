<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <%= link_to 'Trip Memories', root_path, class: 'navbar-brand' %>
    </div>
      <ul class="nav navbar-nav pull-right">
        <% if user_signed_in? %>

         <li class="dropdown">
            <a class="dropdown-toggle nav-subtitle" data-toggle="dropdown" href="#">My Trips
              <span class="caret"></span>
            </a>
             <ul class="dropdown-menu" role="menu">
              <li><%= link_to 'My Trips', trips_path %></li>
              <li role="separator" class="divider"></li>
              <li><%= link_to 'New Trip', new_trip_path %></li>
            </ul>
          </li>

          <li class="dropdown">
            <a class="dropdown-toggle nav-subtitle" data-toggle="dropdown" href="#">
              <%= current_user.name %>
              <span class="caret"></span>
              <ul class="dropdown-menu" role="menu">
                <li><%= link_to 'Profile', edit_user_registration_path %></li>
                <li role="separator" class="divider"></li>
                <li><%= link_to 'Log out', destroy_user_session_path, method: :delete%></li>
              </ul>
                <li class="round-image-50"><%= image_tag(current_user.avatar.url(:thumb)) %></li>
            </a>
          </li>
        <% else %>
          <li><%= link_to 'Log In', new_user_session_path %></li>
          <li><%= link_to 'Sign Up', new_user_registration_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>