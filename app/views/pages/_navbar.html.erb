<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid col-xs-12">
    <div class="trip-memories-container">
        <div>
          <%= link_to 'Trip Memories', root_path, id: 'trip-memories-link' %><br><span class="hide-nav"><img class=" plane" src="https://s3.amazonaws.com/trip-memories/grayplane.jpg"></span>
            <% if user_signed_in? %>
              <span class="hide-nav"><%= image_tag current_user.avatar, class:"round-image-50 avatar" %></span>
            <% else %>
              <span class="hide-nav"><img class="logo " src="https://s3.amazonaws.com/trip-memories/planeworld2.jpg"></span>
            <% end %>  
          <span class="hide-nav"><img class="plane " src="https://s3.amazonaws.com/trip-memories/grayplane.jpg"></span>
        </div>
    </div>
    <div class="col-xs-12">
      <ul class="nav login-signup-ul ">
        <% if user_signed_in? %>
        <div class="container"  id="subtitle-container">
          <li class="col-xs-4 trip-btns border-pink "><%= link_to 'My Trips', trips_path %></li>
          <li class="col-xs-4 trip-btns border-pink"><%= link_to 'New Trip', new_trip_path %></li>
          <li class="dropdown trip-btns col-xs-4 border-pink">
            <a class="dropdown-toggle " data-toggle="dropdown" href="#">
              <%= current_user.name %> 
              <span class="caret"></span>
              <ul class="dropdown-menu trip-btns username border-pink" role="menu">
                <li><%= link_to 'Profile', edit_user_registration_path %></li>
                <li role="separator" class="divider"></li>
                <li><%= link_to 'Log out', destroy_user_session_path, method: :delete%></li>
              </ul>
            </a>
          </li>
        </div>
        <% else %>
          <li class="col-sm-4 col-xs-2"></li>
          <li class=" col-sm-2 col-xs-4 login-signup border-pink"><%= link_to 'Log In', new_user_session_path %></li>
          <li class=" col-sm-2 col-xs-4 login-signup border-pink"><%= link_to 'Sign Up', new_user_registration_path %></li>
          <li class="col-sm-4 col-xs-2"></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>

